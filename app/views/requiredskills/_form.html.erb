<%= form_for(@requiredskill) do |f| %>
  <% if @requiredskill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@requiredskill.errors.count, "error") %> prohibited this expertise from being saved:</h2>

      <ul>
      <% @requiredskill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>